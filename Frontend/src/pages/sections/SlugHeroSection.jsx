import "./SlugHeroSection.scss";

const SlugHeroSection = () => {
  return (
    <section className="slug-hero-section">
      <div className="slug-container">
        <h2>Hero Section</h2>
        <p>
          Cover image, salon logo, salon name, rating,
          address, categories and booking button.
        </p>
      </div>
    </section>
  );
};

export default SlugHeroSection;